# Git Memory Layer

Init: 2026-05-18. Conversation with Randoooos.

Workflow:
- pre-task: `git pull --ff-only`
- post-output: `git add -A && git commit -m "cat: msg" && git push`

Categories:
- work — new feature, output, analysis
- fix — bug correction
- docs — notes, commentary, markdown
- chore — config, tooling, scripts

Only commits on material output, not conversation.
