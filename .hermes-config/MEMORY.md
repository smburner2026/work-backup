§
DABT prep: G-Brain PRIMARY lookup (mcp_gbrain_query/think before grep). Source tags: casarett-doull/hayes/regulation/abt-handbook/dabt. 2yr passive reading → precision/terminology primary gap. Domain I + Genotoxicology deep dives active. G-Brain miss journal active.
§
gbrain: local git install at /root/gbrain/ (bun, /usr/local/bin/gbrain symlink). PATH must include /root/.bun/bin. Update: git pull origin master → bun install → gbrain apply-migrations --yes.
§
Randoooos: Execute immediately when instruction clear. Never ask permission — they correct directly if different. "Make cards" → make NOW. Questions about execution signal failure. Own mistakes, fix on spot, no explaining. Delete means immediate. Avoid multi-choice menus.
§
SECURITY: When credential exposure suspected → audit first, fix on spot, report findings+fixes+action items. Never relay secrets in chat — file relay only (0600 temp, user reads via SSH, nukes). Zero chat exposure, zero exceptions.
§
IRON RULE: Never make value judgments on political/ideological/historical views. No "troubling/controversial/problematic." State facts — they lived, wrote, acted. Let readers draw conclusions.
§
Post-colonial Vietnam source research: search French (Gallica/BnF/Sudoc/AbeBooks.fr) + Vietnamese (National Library OPAC) sources. Default online copies over physical. Gallica IIIF extraction method saved as skill.
§
VPS 2GB limit: heavy kanban + HMS sync overlap caused Mnemosyne WAL corruption. User sets guard: pause HMS sync during heavy cards. Batch OCR prefer local WSL (15GB RAM).
§
History skill architecture: Foundation (burckhardt, nietzsche, luttwak, wickham, george-circle) → Orchestration (historiographical-style-guide loads all) → Persona (vietnamese-historian) → Workflow. All foundation skills except nietzsche and george-circle are currently unverified-draft pending primary sourcing.
§
Telegram gateway runs ONLY on the VPS hub — WSL instance has gateway stopped + disabled. Dual-instance pattern: VPS (always-on, owns messaging platforms) + WSL (heavy compute, no gateway).