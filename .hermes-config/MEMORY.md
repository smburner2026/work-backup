GIT: smburner→gh:work-backup(SSH). Pre-pull→commit+push. Artifacts only.
§
EUPHY: TempMoon→fast minimal entries. euphy-add(1call, no read/patch). Brief ack.
§
SOUL: 4layers. L0=Charter(stance,accountability,pushback,autonomy,standards,escalation,self-improv,endstate). L1=Hermes loops(perm). L2=Karpathy(perm). L3=Costin(swappable). Charter: "You are [Name], my autonomous operator." Never "thought partner." Discuss sections, not draft. Check ExecPrinciples for redundancy.
§
TZ: TempMoon US Central(CDT UTC-5). Server UTC. Cron: CT→+5(CDT)/+6(CST).
§
VPS 2GB+1GB swap. Workspace d3000, Dashboard d9119. Node /root/.hermes/node/bin/. Kanban workers need toolsets beyond hermes-cli.
§
Lesson: "Show me a prompt" = plan review, not permission to execute. Wait for "go ahead" / "fire it".
§
PDF builds: WeasyPrint overflow-wrap:break-word. Liberation Serif 10pt 6x9, per-work+pdfunite vs OOM. Sources at /root/work/nietzsche-anthology/sources/ (10 PDFs+7 EPUBs+TXTs). Nietzsche Complete Works 7.8MB 4402pp done. Nolte book chapters at /root/work/scripts/nolte_md/ + compile_book.py. File transfer: MEDIA fails for large files — user pulls via Tailscale (100.113.2.25) with rsync/scp, give raw one-line commands.
§
LCM installed: plugins/context_engine/lcm@v0.11.1, context.engine: lcm active. Loses no messages — DAG-based context engine replaces flat compression. lcm_grep/lcm_expand/lcm_describe tools available.
§
Memory label trap: before repeating a label from memory about the user ("you use Monte Carlo"), verify it against what they actually say. If there's a mismatch between memory and their current claim, flag it. Don't let stale labels propagate silently — the user's own memory is the source of truth, not my notes from a past session.
§
TRADER PROFILER PROJECT: 7 traders to profile — Trader XO, Husky XBT, doc XBT, Trader Magus, Crypto Chase, Trader Mercury, Pierre. Phase 1(profiles→backtests→Pine Script indicators) mandatory before Phase 2(execution). Non-negotiable sequence.
§
PHONE: Pixel 7a flash successful (CP1A.260505.005, May 2026). Bootloader locked. Issues resolved. MOBILE PREF: raw code blocks only — "Just the raw text" means no prose.