def fibonacci(limit=100):
    """Print Fibonacci numbers up to the given limit."""
    a, b = 0, 1
    while a <= limit:
        print(a)
        a, b = b, a + b

if __name__ == "__main__":
    fibonacci(100)
